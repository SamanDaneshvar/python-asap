# In development, when building the site locally, enter the following command in Ruby:
#     bundle exec jekyll serve --config _config.yml,_config-dev.yml
# Settings in later config files override settings in earlier files.
# In production, GitHub Pages uses the default _config.yml file to build the website.

# localhost
url: http://127.0.0.1:4000
